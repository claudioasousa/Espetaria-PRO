
-- EXECUTE ESTES COMANDOS NO SEU BANCO PARA ADICIONAR AS COLUNAS NECESSÁRIAS:
ALTER TABLE orders ADD COLUMN payment_method VARCHAR(30);
ALTER TABLE orders ADD COLUMN amount_paid DECIMAL(10, 2);

-- Estrutura completa para referência:
/*
CREATE TABLE orders (
    id VARCHAR(36) PRIMARY KEY,
    table_number INT NOT NULL,
    employee_id VARCHAR(36),
    status ENUM('PENDENTE', 'PREPARANDO', 'PRONTO', 'ENTREGUE', 'PAGO') DEFAULT 'PENDENTE',
    payment_method VARCHAR(30),
    amount_paid DECIMAL(10, 2),
    total_amount DECIMAL(10, 2) DEFAULT 0.00,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (table_number) REFERENCES tables(number),
    FOREIGN KEY (employee_id) REFERENCES employees(id)
);
*/
